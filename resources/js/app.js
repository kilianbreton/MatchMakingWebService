import './bootstrap';
import './pages/queues';
import './bootstrap';
import initDropdowns from './utils/dropdown';

document.addEventListener('DOMContentLoaded', () => {
    initDropdowns();
});

import './pages/home';

document.addEventListener('DOMContentLoaded', () => {
    initDropdowns();
});